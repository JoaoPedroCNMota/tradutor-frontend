import { createTheme } from '@material-ui/core/styles';

const Theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#00796b',
    },
    secondary: {
      main: '#5da4ec',
    },
  },
});


export default Theme;